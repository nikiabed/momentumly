import { DatePickerField } from "../../../DatePickerField";

export const DeadlinePicker = (
  props: React.ComponentProps<typeof DatePickerField>,
) => {
  return <DatePickerField {...props} label="ددلاین" />;
};
